<script>
  /*window.onload = () => {
    if(window.openCart) {
      let campaingsInterval = setInterval(() => {
        let gift_exist = document.querySelector('.mu-cart-item + .mu-border');
        if(!gift_exist) {
          fetch(window.location.origin + "/cart.js")
          .then((res) => res.json())
          .then((response) => {
            window.monster_setCartItems(response.items);
            window.mu_productionAddToCartCallBack(response.items);
          })
          .catch((error) => {
            console.log(error);
          });
        } else {
          clearInterval(campaingsInterval);
        }
      }, 1000);
    }
  }*/
</script>